@model IEnumerable<_20170407_ClinicaVet.Models.Consultas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Animal.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Veterinario.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataConsulta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Animal.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Veterinario.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataConsulta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConsultaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConsultaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConsultaID })
        </td>
    </tr>
}

</table>
